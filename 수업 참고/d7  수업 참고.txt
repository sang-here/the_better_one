함수 설명)
함수를 모두 pure 체크하면 안됨(함수 구성에 set 처리가 있으면 반드시 pure 체크 안 하고 실행라인 연결해야 함/weil 뭐가 먼저 실행해야 하는 지 flow를 지정해야하기 때문)
pure 설정 = 참조만 하는 때일 때
category = 분류 정리
description = 함수 마우스 올리면 블루프린트에서 설명문
keyword = 블루프린트 우클릭 검색시 연결될 수 있는 키워드
call in eiditor = 월드에서 설정 조작 가능
access specifier = public 다른 블루프린트에서도 사용 가능
                        protective 상속 받은 부모 자손 관계에서만 사용 가능
                         private 해당 액터의 블루프린트에서만 사용 가능

블루프린트에서 우클릭 후 함수로 접기(collapse to function)

함수는 실행이 내부에 끝나면 자동으로 블루프린트에서 다음 뒤 Exec으로 신호 나옴

개발자는 결국 이해를 기반으로 응용해야 함. 다 외울 수 없을만큼 정보 및 코드 기술이 방대함


블루프린트 인터페이스 만들기(인터페이스=use코드 허브)
컨텐츠 드로어 - 우클릭 - 블루브린트(블루프린트 클래스 아님) - 블루프린트 인터페이스

각 블루프린트에서 인터페이스 불러오기
블루프린트 - 클래스 세팅 - 디테일 - implemented interfaces(인터페이스 구현) - 인터페이스 추가- 인터페이스 창에서 더블 클릭하면 이벤트 함수 불러옴

(인터페이스에서 함수 추가하면 여러 블루프린트에서 불러내서 쓸 수 있음)
(인터페이스도 함수라 인풋 아웃풋 변수 추가도 가능/설정시 이벤트 그래프 상에 이벤트가 아니라 블루프린트에 함수로 나옴)


c++은 컴퓨팅계의 영어/러프하게 기본부터 조작하고 싶거나 새로 구현하고 싶으면 이게 가장 결국 효과적일 것
블루프린트는 c++의 재번역

블루프린트 상에서 코드가 복잡하고 길어지면
함수를 추가해서 정리하거나
이벤트그래프를 추가해서 정리할 수도 있음

fruit 1~5에만 오버랩되야만 count 박스가 count & fruit는 사라짐

월드상에서 복제한 액터는 임시로 존재하며 블루프린트는 하나. 그러므로 그 블루프린트 전체를 그냥 하나의 변수로 지정 저장하여 인스턴스를 키고 월드에서 하나 하나 따로 스위치에 연결해야 함.

destory 액션으로 컴포넌트가 월드에서 사라지는 것 구현 가능

기본창 옆은 액터 배치 place actors