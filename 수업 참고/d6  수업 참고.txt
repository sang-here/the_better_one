코드와 폴더를 진심으로 정리 잘하시오. 후에 크게 당하오. 잘 짜온 코드를 잘 기록 및 보관
이벤트는 오버라이드/ 메쉬 우클릭 혹은 디테일 아래

블루프린트 인스턴스를 켜야 월드에서 디폴트 설정이 가능 / 두 객체가 다있어야 none이 아니라 선택 가능

스위치는 사용자 / 기능이 스마트폰/ 스마트폰을 보통 먼저 만듦

array(배열)는 묶음

for loop을 두개 겹치면 이중구문 (*곱셈될 수 있음) / 주의 / 그땐 sequence 이용

액터가 되어야 트랜스폼(위치값,회전값,스케일값)을 가진다.
cast = 너 뉘기야 = 납치

exercise)
0 2 4 6 8 T
1 3 5 7 9 F

이름지정시 대문자 오면 블루프린트 상에서 자동으로 띄어쓰기 해줌. ex) HiThere = Hi There

delta seconds = 다 더하면 총 시간. 발생한 이벤트 간 간격의 통합 = 사실상 그냥 시간과도 같음
floor 내림
ceil 올림
round 반올림
modulo 
Thus 21 modulo 9 is 3, because when 21 is divided by 9, the remainder is 3.
21 is congruent to 3 modulo 9.

exercise)
0분 0초

코드가 복잡하면 함수를 만드는 편이 지속가능함
함수는 블루프린트 상 액션으로 뽑아내면, 디테일 창에서 퓨어(pure)로 귀엽게 만들 수 있음